cmake_minimum_required(VERSION 3.28)

project(KSD)

#Setting up Config Variables for cmake
set(DEPENDENCIES_BUILD_FOLDER "${PROJECT_SOURCE_DIR}/thirdparty/" CACHE STRING "Build folder for dependecies")
set(SOURCE_FOLDER "${PROJECT_SOURCE_DIR}/src/" CACHE STRING "source folder for library")
set(DEMOS_FOLDER "${PROJECT_SOURCE_DIR}/demo/" CACHE STRING "demos folder for Demonstration usages")

#Donload dependencies
add_subdirectory(${DEPENDENCIES_BUILD_FOLDER})
#Building library and API
add_subdirectory(${SOURCE_FOLDER})
#Building demos
add_subdirectory(${DEMOS_FOLDER})
#Building tests
